// PostCSS configuration for Tailwind CSS
// Using built-in Tailwind CSS v4 optimization features instead of PurgeCSS

export default {
  plugins: [
    // Tailwind CSS v4 includes built-in optimization
    // No need for additional PurgeCSS plugin
  ]
}